---
argument-hint: <file-path>
---

Write tests for the file at `$ARGUMENTS`.

- Create the test file inside a `__tests__` folder within the same folder as the source file, named `<filename>.test.ts` or `<filename>.test.tsx` depending on the file type.
- Cover all success, error, and edge-case scenarios.